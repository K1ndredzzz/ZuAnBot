<!-- 批量编辑对话框 -->
<UserControl x:Class="ZuAnBot_Wpf.Views.BatchEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ZuAnBot_Wpf.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism ="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">
  <prism:Dialog.WindowStyle>
    <Style TargetType="Window">
      <Setter Property="Height" Value="500"/>
      <Setter Property="Width" Value="600"/>
      <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen"/>
    </Style>
  </prism:Dialog.WindowStyle>

  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="多行编辑 - 每行一条词句" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>

    <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
      <TextBox x:Name="textBox_content"
               Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto"
               HorizontalScrollBarVisibility="Auto"
               FontSize="12"
               Padding="5"/>
    </Border>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,5">
      <TextBlock Text="提示：每行一条词句，空行将被忽略" Foreground="Gray" FontSize="11"/>
      <TextBlock Text="{Binding WordCount, StringFormat='当前词条数：{0}'}" Margin="20,0,0,0" Foreground="DarkGreen" FontWeight="Bold"/>
    </StackPanel>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="保存" Command="{Binding SaveCommand}" IsDefault="True" Style="{StaticResource ButtonPrimary}" Width="100" Margin="0,0,10,0"/>
      <Button Content="取消" Command="{Binding CancelCommand}" IsCancel="True" Width="100"/>
    </StackPanel>
  </Grid>
</UserControl>
